version: "3.8"

services:
  julia:
    build:
      dockerfile: Dockerfile
      context: ./julia
    environment:
      - NODE_ENV=production
      - PORT=5000
  app:
    build:
      dockerfile: Dockerfile
      context: ./app
    volumes:
      - /app/node_modules
      - /app/src/styles
      - ./app:/app
    environment:
      - NODE_ENV=production
      - DATABASE_URL="${CO2_PRICING_DATABASE_URL}"
      - NEXTAUTH_SECRET=53cr37
      - NEXTAUTH_URL=http://localhost:3000
