import type { CalculatorStage } from "../../types/calculator/stage";

export const CALCULATOR_STAGES: CalculatorStage[] = [
    {
        id: 'select-iam-model',
        name: 'Select IAM model',
        description: 'Select IAM model from the list'
    }
]
